{
  "name": "urbanpulse-guardian-ai",
  "version": "2.0.0",
  "description": "UrbanPulse Guardian AI - A React and Node.js Migration",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend_react",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "npm install && npm install --prefix frontend_react && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
